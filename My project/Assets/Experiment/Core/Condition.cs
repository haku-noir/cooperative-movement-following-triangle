namespace FollowingTriangle.Core
{
    /// <summary>
    /// 実験1の 3 条件（仕様書 §0.2）。
    ///   C1: 頂点のみ + 「3 つの点を合わせてください」
    ///   C2: 頂点のみ + 「3 点を三角形と見なし、その三角形を合わせてください」
    ///   C3: 頂点 + 辺 + 「三角形を合わせてください」
    ///
    /// C1 と C2 の差は教示文のみであり、描画は完全に同一でなければならない。
    /// 教示文はコードに埋め込まず外部 JSON に置く（仕様書 §7.4、段階5で実装）。
    /// </summary>
    public enum Condition
    {
        C1 = 1,
        C2 = 2,
        C3 = 3,
    }
}
